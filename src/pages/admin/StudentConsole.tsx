import React from 'react';
import { StudentConsoleFeature } from '../../admin/features/students/components/StudentConsoleFeature';

export const StudentConsole: React.FC = () => {
  return <StudentConsoleFeature />;
};
